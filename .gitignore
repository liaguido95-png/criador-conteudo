# Arquivos de sistema operacional
Thumbs.db
ehthumbs.db
desktop.ini
.DS_Store
.DS_Store?
._*
$RECYCLE.BIN/

# Editores / IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# Segredos e configuração local (nunca commitar chaves privadas, mesmo que
# o apiKey público do Firebase não seja segredo — ver R16 do CLAUDE.md)
.env
.env.*
*.local

# Firebase CLI (cache e logs gerados ao rodar `firebase deploy`/`firebase serve`)
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebaserc.local

# Logs diversos
*.log
npm-debug.log*

# Backups e rascunhos temporários de edição
*.bak
*.tmp
*~$*
